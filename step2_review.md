# Step 2 Review

- 文件用途：记录 Step 2 最终收尾状态，说明这 30 篇样本集已经达到可进入后续开发的程度。
- 什么时候使用：准备从 Step 2 切换到 Step 3 前，快速确认样本集状态时使用。
- 我应该怎么维护：后续如无新的人工核对轮次，不再改动本文件的结论性描述。

## Step 2 Final Status

Step 2 已完成。

当前统计：
- 总样本数：30
- `done`：18
- `needs_review`：12

## 当前样本集如何使用

- `done` 样本可直接作为高置信真值使用。
- `needs_review` 样本虽然保留审慎标记，但用户已接受当前保守标注结果，因此也可作为保守真值使用。
- 两个子集现在都可以进入后续开发与回归阶段；区别只在置信度层次，不在可用性。

## Step 2 已完成的内容

- 已完成 30 篇 PDF 的样本落位与主表整理。
- 已完成首页作者区与邮箱区的首轮人工核对。
- 已完成 `done` / `needs_review` 的区分。
- 已为存在模糊点的样本保留单篇 note。
- 已形成可用于开发与回归的工作样本集。

## 用户后续不再需要做的事

- 不需要继续强行消除这 12 个 `needs_review` 样本。
- 不需要重跑整批 Step 2 标注。
- 不需要把 `needs_review` 样本排除在后续开发之外。

## 可进入 Step 3 的依据

- 作者顺序字段已形成稳定真值基础。
- `first_author` 已统一按 V1 规则落地。
- 通讯作者、共同一作、共享邮箱/分组邮箱等关键难点都有覆盖。
- `needs_review` 样本已被用户接受为保守真值，因此不会阻塞后续开发。

## 当前没有做什么

- 本轮没有进入 Step 3 业务实现。
- 没有新增 PDF 解析、作者抽取、邮箱抽取、匹配或地域分类代码。